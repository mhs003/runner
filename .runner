@vars:
  BIN = app
  TIN = 234623

@cat:
  runner_save = .runner

main:
  $ build dev:backend
  echo "Done!"


runner_save:
  echo "{{runner_save}}" > test.txt

hello:
  echo "test test"
  echo "hello world"

dev:backend:
  echo "Backend dev server started at port :9091"

dev:frontend:
  echo "frontend dev server started at port :3000"
  
build:
  echo "building '{{BIN}}' in '{{CWD}}/build' for '{{OS}}-{{ARCH}}'"
