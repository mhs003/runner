@vars:
  BUILD_DIR="./build"
  BIN="run"
  ENTRY="./cmd/run"

main:
  @ build

build:
  mkdir -p {{BUILD_DIR}}
  !go build -o {{BUILD_DIR}}/{{BIN}} {{ENTRY}}
  # ! = enable verbose

install:
  !cp {{BUILD_DIR}}/{{BIN}} ~/.config/hypr/bin/run

run:
  {{BUILD_DIR}}/{{BIN}}